pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
                checkout scm
            }
        }
        stage('Deploy') {
            steps {
              dir('/backend') {
                sh 'docker-compose -p edubox-backend up -d --build'
            }
        }
    }
}
}
