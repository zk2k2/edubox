pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
                
                sh 'cp target/backend-0.0.1-SNAPSHOT.jar ${WORKSPACE}/'
            }
        }

        stage('Deploy'){
          steps{
            sh 'docker-compose -p edubox-backend up -d --build'
          }
        }
        
    }
}
